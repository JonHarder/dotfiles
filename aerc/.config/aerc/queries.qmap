today = date:today and tag:inbox
inbox = tag:inbox
archive = not tag:deleted and not tag:sent
trash = tag:deleted
unread = tag:unread and tag:inbox
sent = tag:sent and not tag:deleted
flagged = tag:flagged and not tag:deleted
